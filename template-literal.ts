(function () {
    function getEdad(){
        return 100 + 100 + 300;
    }
  const nombre = "Matias";
  const apellido = "Troncoso";
  const edad = 32;

  const salida = 
  `${nombre}
   ${apellido} 
   (${getEdad()})
   `;
  console.log(salida);
})();
